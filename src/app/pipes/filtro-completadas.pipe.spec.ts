import { FiltroCompletadasPipe } from './filtro-completadas.pipe';

describe('FiltroCompletadasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCompletadasPipe();
    expect(pipe).toBeTruthy();
  });
});
